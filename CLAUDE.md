# CLAUDE.md — kabulab mono-repo 開発ルール

このリポジトリは [kabulab](https://kabulab-cf.satoki252595.workers.dev/) — 日本株投資ツール統合ポータル。
詳細は [README.md](./README.md) と [docs/overview.md](./docs/overview.md) を参照。

このファイルは Claude Code セッション開始時に自動で読み込まれる。本プロジェクトの全実装作業に適用される絶対ルールを以下に記す。違反した場合は実装をやり直す。

個別サービスのコーディング規約は各 [`services/<slug>/CLAUDE.md`](./services/) に分けて記載する。

---

## 🚫 ルール1: ダミーデータ・サンプルデータの利用禁止

実コードパス・実出力に **架空の値** を埋め込まない。

### 禁止されること (ルール1)

- 「とりあえず動かすために」プレースホルダのテキスト・画像・**数値**・人名・住所・日付などをそのまま画面/API応答/永続化レイヤに出す
- 推測・生成された値を、その由来を明示せずに本物のデータと混ぜて表示する
- フィクションの固有名詞 (架空の人名・店名・URL) を実データと並べて表示する
- `placeholderX()` / `dummyX()` / `mockX()` のような生成関数を本番経路から呼び出す

### 唯一の例外: 「サンプル」が機能要件として明示されている場合

プロダクトのコア機能として「サンプル品の配布」「デモ環境」「プレビュー」などが明示的に定義されている場合に限り例外を認める。ただし以下を全て満たすこと:

1. 配布物・画面上に **明示的な "SAMPLE" / "DEMO" / "PREVIEW" バッジ** が視覚的に表示されている
2. 推定/生成データには `is_placeholder: true` などの **機械可読フラグ** が必ず付く
3. 関係者向けドキュメント (メール本文・README など) に **「最終承認/差し替えが必須」と明記** される
4. 承認前は公開状態にしない (`status='draft'` などで隔離)

つまり「サンプル」と「本番」の境界を **型と画面表示で明確に分離** すること。境界を曖昧にしたまま値を流すのは禁止。

### 違反検知のチェックポイント (ルール1)

- `placeholder` / `sample` / `dummy` / `mock` / `fake` を含む変数名・関数名が本番経路に到達していないか (grep で検査)
- 生成データが識別フラグなしで DB / ストレージに保存されていないか
- 本番URL/エンドポイントで SAMPLE 表示なしに架空値が露出していないか

---

## 🚫 ルール2: フォールバック処理の禁止

期待する値が無い・取得失敗した時に、**別の値で黙って埋める処理を書かない**。

### 禁止されること (ルール2)

- `?? "—"` / `?? "未設定"` / `?? defaultValue` のような silent substitution
- `try { ... } catch { return defaultValue; }` で例外を握りつぶしてデフォルトを返す
- `if (!data) data = synthesize();` で欠損を勝手に補う
- `switch` 文の `default:` で「分からない時の値」を返す
- 候補が見つからない時に「とりあえずの第一候補」を返す `select` / `pick` 関数
- 外部APIの応答が想定外の時に、無効な値で埋めて続行する
- LLM等の確率的応答が schema に合わない時、無効値で埋めて続行する

### 代わりに採るべき方針

1. **早期に失敗** (`throw new Error(...)`): 期待値がない時点で例外を投げ、処理を止める
2. **明示的に "未取得" を型で表現**: `T | undefined` / `Option<T>` / `Result<T, E>` で呼び出し側に判断を委ねる。`??` で消さない
3. **欠損は欠損のまま画面に表示**: 「未設定」「データなし」と明示する。架空値で埋めない
4. **オペレータに通知**: ログ警告・監視ダッシュボード・アラートで人間が気づく経路を必ず作る

### 例外として認める唯一のケース

ユーザー入力の正規化 (例: 電話番号のハイフン削除、空白 trim、大文字小文字統一) は許容。これは「同じ意味の値の表現揺れを揃える」処理であってフォールバックではない。

### 違反検知のチェックポイント (ルール2)

- 新規コードで `??` を見たら立ち止まる: 「これは正規化か? 黙ってデフォルト値を入れていないか?」を自問
- `try/catch` で `return defaultValue` を返している箇所は全て要検査
- `select_*()` / `pick_*()` / `find_*()` 系で「見つからなければ X を返す」になっていないか
- `switch` の `default:` が「想定外を吸収する」ためだけに存在していないか

---

## 🔐 ルール3: 環境変数は .env で管理・取得する

機密値・環境固有値は `.env` (リポジトリルート) でのみ管理し、コード内では型付きアクセサ経由で取得する。

### 禁止されること (ルール3)

- ハードコードされたシークレット・APIキー・URL・メールアドレス・人名・住所・電話番号
- ソース内の `?? "デフォルト値"` で「環境変数が設定されていない時の値」を埋める (ルール2との重複違反)
- 別ファイル (`config.json` / `secrets.ts` / `constants.ts` 等) に値を直書きする
- 同じ環境変数を複数箇所で `process.env.X` 直参照する (誤記事故防止のため必ず getter 経由)

### 必須仕様

1. **`.env` のみが正のソース**: 本番値・開発値・テスト値の差は全て `.env` で管理
2. **`.env` は `.gitignore` 必須**: `.env`, `.env.local`, `.env.*.local` などを除外
3. **`.env.example` を必ず提供**: 本番で必要な全変数のキーをリストし、値は `<例>` プレースホルダ。新メンバが `.env.example` をコピーするだけで開発を始められる状態を維持
4. **型付きアクセサに集約** (`lib/env.ts` 等): 全 `process.env.*` 直参照を集約し、未設定時は **起動時/初回参照時に throw** する
5. **ホスティング環境固有の管理機構を尊重**: 例えばサーバーレス/コンテナ環境ではプラットフォームのシークレット管理 (Cloudflare Workers の Secrets、AWS Parameter Store、Vercel Env Variables 等) を併用。`.env` ファイルが読まれない環境ではそちらが正のソース
6. **CLI 起動時の env ロードを明示**: ランタイム (Node / Bun / Deno等) ごとの env-file 読み込み方法を `package.json` の scripts および README の起動例で明示

### 型付きアクセサの設計例 (言語非依存の原則)

```ts
function required(key: string): string {
  const v = process.env[key];
  if (!v || v.trim() === "") {
    throw new Error(`環境変数 ${key} が設定されていません。.env を確認してください。`);
  }
  return v;
}
function optional(key: string): string | undefined {
  const v = process.env[key];
  return v && v.trim() !== "" ? v : undefined;
}

export const env = {
  API_KEY: () => required("API_KEY"),
  OPTIONAL_FEATURE_FLAG: () => optional("OPTIONAL_FEATURE_FLAG"),
};
```

呼出側は `env.API_KEY()` のように関数呼び出し。取得時に未設定なら例外でフォールバックしない (ルール2との一貫性)。

### 違反検知のチェックポイント (ルール3)

- `process.env.` (または各言語の同等物) を集約モジュール以外で直接書いていないか
- `??` でデフォルト値が付いていないか
- `.env` がコミット履歴に紛れ込んでいないか (`git log --diff-filter=A --name-only | grep -E "^\.env$"`)
- 新規追加した環境変数は `.env.example` にも必ず追記したか

---

## ルール4: (2026-09-14 に削除)

旧「専門エージェントによる多角精査」。参照していた `.claude/agents/` が
リポジトリに存在せず実行不能だったため削除 (経緯は git 履歴)。
レビューはルール5 の PR 上で行う (番号は詰めない — ルール6/7 の参照を保つ)。

---

## 🚀 ルール5: 修正完了後は PR を作る

main への直接 push はしない。ユーザに次の指示を仰ぐ前に以下を完遂する:

1. feature ブランチを切る (`git checkout -b <type>/<slug>`)
2. `git status` / `git diff` で全変更を確認
3. **関連ファイルだけを `git add`** (ワイルドカードで秘匿ファイル巻き込み禁止 — ルール3整合)
4. **コミットメッセージ** は変更の "why" を1-2文で記述。Conventional Commits 風の prefix (`feat:`, `fix:`, `refactor:`, `chore:`, `docs:`, `perf:`, `test:`) を使う
5. `git commit` (デフォルト Co-Authored-By トレーラー付与で OK) → `git push -u origin <branch>`
6. `gh pr create` で main 宛ての PR を作る。題名は `type(scope): 日本語の要約`
7. CI が緑になるまで見届ける。レビュー指摘・CI 失敗は直して push (PR 上で反証レビュー)
8. 緑になったら `gh pr merge --squash` でマージする (ユーザが「マージして」と明示した場合を除き、マージ自体はユーザ判断に委ねてもよい)
9. **失敗** (pre-commit hook / lint / push reject) した場合: 原因を解析して直す。`--no-verify` で hook をバイパスしない。`--force` も禁止 (組織の運用規程で明示許可されている場合のみ例外)

### remote が未設定 / ブランチ初コミットの場合

- `git remote -v` で remote が空 → ユーザに「リポジトリ作成と remote 追加 (`git remote add origin <url>`) をお願いします」と報告し、その時点で停止
- リポジトリが `git init` 未実施 → ユーザに状況を報告し、init + remote 設定を合意してから着手

### 例外 (PR 化をスキップしてよいケース)

- ユーザが「commit だけして」「ローカルだけで止めて」と明示した場合

---

## 🗄️ ルール6: 一次データは Notion に必ずアーカイブする

外部 API / スクレイピング / ファイル取得で得た **一次データ (raw source
data)** は、DB へ構造化保存するのとは別に、**メタデータとともに Notion の
「バックアップ」ページ配下のサービス別データベースへ冪等記録** する。
**ファイルを取得した場合は、その物理ファイルを Notion へ実体アップロード
する** (URL 参照や要約ではなく、バイト列そのものを上げる)。

唯一の実装窓口は共有ライブラリ
[`src/shared/notion-archive/`](./src/shared/notion-archive/) の
`recordPrimaryData()` / `moveToTrash()` / `isArchived()`。各サービスは
これを呼ぶだけにし、Notion REST を個別に叩かない。

### 禁止されること (ルール6)

- ファイルを取得した経路で、物理バイト列を上げず **URL 参照・要約・件数
  だけ** で「記録した」ことにする
- `notion-archive/` を迂回して `api.notion.com` を個別に叩く (レート制御の
  一元化を壊す)
- input 変更で不要化した元データを `moveToTrash()` を通さず **そのまま
  上書き/削除** する (なぜ不要化したかの追跡を失わせる)
- Notion 上限超過時に、適当なダミーファイル/切り詰めた値で埋めて
  「成功」扱いにする (ルール2 違反。正直なステータスを記録する)
- 一次データ DB を手で作って命名規約 (下表) から外す

### 記録先 (固定)

| 用途 | Notion ページ | 環境変数 |
|---|---|---|
| 一次データ本体・銘柄別データ・不要化した元データの退避先 | 「一次データ保管」 | `NOTION_ARCHIVE_PAGE_ID` |

(2026-09-25 に「バックアップ」「ごみ」の2ページ運用から「一次データ保管」
1ページへ統合した。経緯: 旧「バックアップ」ページは銘柄コード毎の子ページ+
子DB が数千件累積して Notion 上で開けなくなり、ユーザが当該ページを
トラッシュする事態になった。移行は `scripts/notion/relocate-archive.ts` で
実施し、旧ページ ID は `NOTION_BACKUP_PAGE_ID`/`NOTION_TRASH_PAGE_ID` として
移行専用に一時的に残す。)

- ページ配下に `一次データ｜<service>` / `銘柄一覧｜<service>` /
  `ごみ｜<service>` という **サービス別 DB** を自動生成して使う。
  区切りは **全角縦棒「｜」(U+FF5C)**。Notion で手検索する際は半角 `|`
  と取り違えないこと。
- `NOTION_TOKEN` / 上記ページ ID は `.env` のみが正のソース。コードからは
  `notion-archive/env.ts` の型付きアクセサ経由でのみ参照 (ルール3整合)。

### 銘柄別データは単一 DB にする。子ページ/子DBを量産しない

銘柄コード等をキーにした行データを Notion に持つときは、**銘柄ごとに
子ページ/子DBを新規作成しない**。全銘柄共通の単一 DB を作り、銘柄コードは
行の1プロパティとして持つ (「有報テキスト」DB — `stock-text.ts` — が正準
実装)。理由: 親ページ配下に子ページ/子DBが数千件累積すると Notion 上で
そのページ自体が開けなくなる (2026-09-25 実際に発生し「バックアップ」
ページのトラッシュに至った)。銘柄別の子ページ階層 (例:
`銘柄一覧｜<service>` → 銘柄ページ → 子DB) が既に存在する箇所
(`dataset.ts` の適時開示等) は既知の例外として残すが、**新規に同種の
階層を増やさない** — 新規実装は単一 DB 方式を選ぶこと。

### 冪等キーと「不要化データ」の扱い

- `key` (例: EDINET docId、`jpx-listing-YYYY-MM-DD`) で冪等。既存 key は
  再アップロードしない (バックフィルが再開可能)。
- **input が変わり、元データが不要になったとき** は破棄せず
  `moveToTrash()` で「ごみ」配下へ **物理ファイルごと退避** し、元レコードは
  Notion ゴミ箱へ送る。退避レコードに `Obsoleted At` / `Obsoleted Reason`
  / `Origin Page` を必ず残す (なぜ不要化したか後追い可能にする)。

### Notion ハードリミットの遵守 (ルール2 整合)

- レート: 平均 ~3 req/s。`notion-archive/client.ts` がプロセス内で全通信を
  直列化し最小間隔を強制、429 は `Retry-After` を尊重、5xx は指数バックオフ。
- ファイル: single_part 20 MiB 以下 / それ超は multi_part / 合計は WS 上限。
  **WS 上限超過は捏造で埋めず** (ルール2)、`Status=file_too_large` という
  正直なステータスと事実 (サイズ) を記録し運用者に可視化する。
- メタデータ全文は rich_text 上限 (2000 字) で欠落させず、ページ本文の
  code block に分割して原文保存する。

### 高頻度・大量取得の境界 (ハードリミット考慮の帰結)

「全銘柄 × 日次」の Yahoo JSON のような **1 取得=1 レコードにすると Notion
の現実的上限を即超過する高頻度ポーリング** は、そのままミラーしない。
これは「Notion のハードリミットを考慮する」要件の帰結である。代わりに:

- **物理ファイルとして降ってくる一次取得物** (EDINET 有報 ZIP、JPX
  上場銘柄 XLS、優待スクレイプの確定 JSONL エクスポート 等) は
  **必ず** `recordPrimaryData()` で実体アップロードする。
- per-stock の派生 JSON は DB が正本。Notion へは「取得バッチ単位の
  確定ファイル/サマリ」粒度で記録する (個別レコードを大量生成しない)。

### 違反検知のチェックポイント (ルール6)

- ファイルを `fetch`/ダウンロードしている経路で `recordPrimaryData()` に
  バイト列を渡しているか。要約や URL だけで済ませていないか。
- Notion REST (`api.notion.com`) を `notion-archive/` 以外から直叩きして
  いないか (レート制御を迂回していないか)。
- 元データを上書き/削除する経路で、不要化分を `moveToTrash()` に通さず
  捨てていないか。

---

## 💡 ルール7: UI の専門用語には投資初心者向けバルーンヘルプを必ず付ける

kabulab のターゲットは**投資初心者を含むモバイル投資家（高齢層含む）**。
画面に出る金融・統計・指標の専門用語は、初心者がその場で意味を理解できる
**バルーンヘルプ（ツールチップ）**を必ず併設する。「分かる人だけ分かればいい」
表示は不可。

### 対象となる用語 (例)

- 指標・統計: CAGR / YoY / RSI / ATR / MACD / β / ボラティリティ / 偏差値 /
  パーセンタイル / シャープレシオ 等
- 財務・制度: 受注高 / 受注残高 / PER / PBR / ROE / 営業利益率 / 配当利回り /
  優待利回り / 時価総額 / EDINET / 有報 等
- そのサービス独自のスコアやフラグ (例: `※起点僅少` `※年欠落` 優待スコア)

「列見出し」「フォーム項目ラベル」「スコア名」「フラグ」など、**ユーザーが
意味を取り違えうる短い語**には原則すべて付ける。

### 操作モデル (固定)

**`?` マークや別アイコンは付けない**。**用語そのもの**を点線アンダーライン
(`border-bottom:2px dotted`) で「説明あり」と示し、その語を
**マウスオーバー / フォーカス / タップ** すると解説バルーンが出る。
001 RSI / 002 お宝優待の `.tip` / `.tip-text` と見た目・挙動を統一する
(別方式の独自トリガを各サービスで作らない)。

### 必須仕様 (ルール7)

1. **平易な日本語で 1〜3 文**。中学生でも分かる言葉に噛み砕く。可能なら
   具体例（数値例）を 1 つ添える（例: 「CAGR=複数年の伸びを1年あたり平均%に
   ならした値。5年で約2倍なら約 +15%/年」）。
2. **定義は財務的に正確に**。噛み砕きと引き換えに誤った定義を教えない
   （ルール1 の精神。フロー/ストックの混同など典型的誤解は明示的に否定する）。
3. **CSS のみ・JS 不要**。`:hover` / `:focus` / `:focus-within` / `:active`
   で開く（PC ホバー・キーボード・モバイルタップを全てカバー）。
4. **モバイル・高齢層対応**: 用語(トリガ)は十分なコントラストの点線下線で
   気付けること、`tabindex="0"` + `role` + `aria-label` でキーボード /
   スクリーンリーダ到達可能、モバイルで `left:0` 等に寄せて画面外に
   出さない（共通実装の media query 済）。`title` 属性のみ（モバイルで
   出ない）は不可。
5. **デザインシステム遵守**: Editorial Swiss Grid（`--bg-invert` 背景 /
   2px 黒枠 / 角丸 `var(--radius)` / 影は `5px 5px 0 0` / グロー・
   ソフトシャドウ禁止 / 配色トークン使用）。
6. **クリップ回避**: `overflow` を持つ祖先（横スクロール表 `.table-wrap`
   など）で切れないよう、共通実装はバルーンを語の **下** に出す。出せない
   時はインライン展開 (`<details>` 等) にフォールバック。

### 正準実装 (これを使う)

[`src/shared/term-tip.ts`](./src/shared/term-tip.ts) の `termTip(label, text)`
ヘルパ + `TERM_TIP_STYLES` が**唯一の正準実装**。新規・改修では必ずこれを
import して使う（各サービスで `.tip` を再定義しない）。利用例は
`services/yuho-quant/src/views/screening.ts`。
※ 001 RSI / 002 お宝優待は本ルール制定前から layout 内に同等の `.tip` を
インライン保持している（挙動は同一）。これらは将来 `term-tip.ts` へ統一
予定の既知の重複で、改修で触れた際に移行してよい（新規重複は作らない）。

### 違反検知のチェックポイント (ルール7)

- 新規・改修した画面に、初見では意味不明な略語・指標名が**ヘルプ無し**で
  出ていないか（列見出し・ラベル・スコア名を一つずつ確認）。
- ヘルプ文が専門家向けの再定義になっていないか（初心者が読んで分かるか）。
- `?` 等の別トリガを付けていないか（語自体が点線下線トリガ）。
- `src/shared/term-tip.ts` を使わず `.tip` を独自再定義していないか。
- `title` 属性のみ（モバイルで出ない）/ JS 依存になっていないか。

---

## このルールが効く範囲

- 新規コードを書くとき
- 既存コードを修正するとき
- バグ調査で「とりあえず動かしたい」と思ったとき(特に注意)
- デモ・PoC・「あとでちゃんと直す」想定のコード(特に特に注意 — ちゃんと直されない)

ルール違反のコードを発見したら、commit 前に必ず修正する。ユーザーに「フォールバック入れますか?」と聞かない — ルール上の答えは常に NO なので、最初から throw / null を使う設計にする。

---

## プロジェクト固有の規約

- **JSX 禁止**: mono-repo 方針として、ビューは template literal を返す `.ts` 関数として実装する (Cloudflare Workers / esbuild ビルドでも全サービスがこの方式を踏襲する)。詳細は [`docs/new-project-template.md`](./docs/new-project-template.md) の §5。
- **Hono のサブアプリ**は必ず `new Hono({ strict: false })` で生成し、trailing slash の有無に依らずマッチさせる。
- **DB アクセス**は必ず Worker の D1 バインディング (`c.env.DB` → `createDb(c.env.DB)` / `createServiceDb`) 経由で取得し、接続先をハードコードしない。取込 (書込) は Node から `createD1HttpDb` (D1 REST) を使う。
- **POST フォームの optional フィールド**は `z.preprocess((v) => v === "" ? undefined : v, ...)` で空文字列を undefined に正規化する (HTML form の標準挙動でフォーム未入力は `""` 送信)。`z.coerce.number().optional()` 単独だと `""` が `0` に変換されるバグの温床。
- **データ層に「初期値・デフォルト値」を埋め込まない** (ルール1帰結): フォームのプリフィル値も `null` で起動し、実データが取得できた場合のみ実値をセット。「とりあえず 100 円」のような根拠不明値は本番経路から消す。

詳細は各 `services/<slug>/CLAUDE.md` および [`docs/overview.md`](./docs/overview.md) を参照。
