# macOS
.DS_Store
**/.DS_Store

# Node.js
node_modules/
**/node_modules/
.pnpm-store/
.pnpm-debug.log*
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build output
dist/
build/
.next/
out/

# Vercel
.vercel/

# Test coverage
coverage/
.nyc_output/

# IDE / Editors
.idea/
.vscode/
*.swp
*.swo
*~

# Environment files (NEVER commit secrets)
.env
.env.local
.env.*.local
!.env.example

# Logs
logs/
*.log

# TypeScript
*.tsbuildinfo

# Drizzle journal/cache
**/drizzle/meta/_journal.json.bak

# Conductor (local agent state)
conductor/

# Claude Code ローカルキャッシュ (codegraph SQLite ~5MB、再生成可能)
.codegraph/

# Temporary files
tmp/
temp/
.vercel

# nix / direnv
.direnv/
result
result-*

# ローカル LLM の GGUF モデルキャッシュ (数 GB。優待解釈 step3 が DL)
services/otakara-yutai/data-scripts/models/

# 優待データの作業ディレクトリ一式。ここには出典サイト由来の掲載文
# (benefit-descriptions.jsonl) と、その解釈の中間生成物 (interpreted/chunk-*.jsonl、
# 原文 description を含む _raw-fail-*.txt) が入る。
# 出典サイトの規約が私的使用を超えた蓄積・公開を禁じており、public リポジトリに
# 載せると画面から掲載文を外しても raw URL で全量を取得できてしまう
# (2026-09-12 に benefit-descriptions.jsonl 5,694行を履歴ごと除去した)。
# 中間生成物は再生成可能で、解釈の正本は step4 で D1 に反映済み。
services/otakara-yutai/data-scripts/data/

# Cloudflare Wrangler
.wrangler/
.dev.vars

# 一度きりの cutover データダンプ (本番データ。コミットしない)
# _cutover-data.sql (yuho/ir) / _cutover-cluster.sql (001-004) /
# _cutover-swing-ohlcv.part*.sql (337k 行 OHLCV) など、生成物は全て除外。
drizzle/d1/_cutover-*.sql
