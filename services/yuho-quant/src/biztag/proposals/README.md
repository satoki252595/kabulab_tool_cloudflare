# 単語帳(語彙)見直し提案 — 保管場所

このディレクトリは `pnpm biztag gate` に**まだ通していない**単語帳変更提案の草稿を置く場所。
`vocabulary/v1.json` には反映しない(単語帳の正本は台帳 DB — `services/yuho-quant/CLAUDE.md` 参照)。

## ここにある提案

- `2026-09-25-quality.json` — 2026-09-25 の精度診断(タグ0件 1,229/3,606件)を受けた品質改善提案。
  `baseVersion: "v1"`。内容は `docs/005-yuho-quant-business-tags.md` 更新時のコミットメッセージ、
  または本エージェントの最終報告を参照。

- 2026-09-26 に上記を提出し、関門で採用 → **v2**。続けて `2026-09-26-tester-themes.json`
  (半導体テスタを半導体製造装置・半導体バリューチェーンのテーマ構成語へ追加) を採用 → **v3**。
  台帳が正本なので、このディレクトリのファイルは提出記録として残す。

- `2026-09-26-round2.json` — v3 適用後もタグ0件が1,128/3,606件残っていた診断への
  是正案(ラウンド2)。`baseVersion: "v3"`。銘柄マスタ（補足）の空タグ・判定済 1,130 社の
  「事業の内容」を精査し、卸売業(127社)・小売業(78社)・サービス業の一部(経営コンサル・
  広告代理業)に単語帳の語が1つも無いことが最大の原因と特定。distribution 列(当時
  10/100・上限まで90語の余地あり)へ5語を新設し(`B.SVC.SPECIALTY_WHOLESALE` 専門商社・
  卸売業／`B.SVC.APPAREL_FOOTWEAR_RETAIL` 衣料品・靴の小売専門店／
  `B.SVC.LIFESTYLE_SPECIALTY_RETAIL` 生活雑貨・趣味用品の小売専門店／
  `B.SVC.MANAGEMENT_CONSULTING` 経営コンサルティング／`B.SVC.ADVERTISING_AGENCY`
  広告代理店)、あわせて実データで「自動車部品」という平易な表記が132件出現するのに
  既存語 `B.MOBI.AUTO_PARTS`(upstream・満杯)の keywords が JSIC の複合語のみだったため
  `add_keywords` で補強した(2026-09-25 タイヤ是正と同種)。出典はすべて日本標準産業分類
  (総務省 R5)。詳細な検証結果は下記「このエージェントが実施した検証」を参照。

- `2026-09-26-v5-core-themes.json` — 利用者報告(9987 スズケンで中核の医薬品卸売事業が
  付かず、周辺事業の物流受託だけを根拠に投資テーマ「港湾ロジスティクス」が付く)を受けた
  是正案。`baseVersion: "v4"`。全銘柄(3,607社・判定済)を横断監査し、次の2系統の問題を
  是正する:
  1. **テーマ構成語が汎用的すぎる**: `T.PORT_LOGISTICS`(港湾ロジスティクス)の構成語
     `B.LOGI.WAREHOUSING_3PL`(倉庫・3PL)・`B.MACH.MATERIAL_HANDLING`(物流機器・マテハン)は
     いずれも定義自体に港湾との関連が無く(前者は「常温・常圧の普通倉庫」全般、後者は
     「工場、倉庫、鉱山」向け搬送機器)、実データでは145社中116社(倉庫・3PL単独90社+
     マテハン単独26社)がこの2語だけを根拠にテーマ付けされ、SGホールディングス・
     ハマキョウレックス等の陸運会社にまで「港湾ロジスティクス」が付いていた。港湾で
     船舶荷役・コンテナターミナル運営を行う `B.LOGI.PORT_OPERATIONS` のみを構成語として残す。
  2. **キーワードが有報の実際の言い回しに当たらない**: `B.SVC.SPECIALTY_WHOLESALE`
     (専門商社・卸売業)の keywords は「卸売業」等だが、有報の事業区分表は「医薬品卸売事業」
     のように「卸売事業」表記を使うことが多く、`add_keywords`で補強。さらに definitionJa/
     definitionEn の例示商品分野に医薬品が無かったため、キーワードで候補になっても
     医薬品卸売各社が「確認不能」止まりになることをドライランで確認し、例示に
     「医薬品・医療用品」を追加する `update` も合わせて行った。
  - 検証結果(下記「このエージェントが実施した検証」の続き): `validateVocabulary` 0件・
    `verifySources` 0件・変更語2件(有効語261件の0.77%)・ゴールデン評価は precisionYes
    悪化なし(v4 0.9649→v5 0.9650、recallYes 0.7597→0.7624)。ドライラン19社
    (医薬品卸5社+多業種卸売8社+港湾テーマ剥落6社)で、スズケン等5社が「専門商社・卸売業」
    を新規に「はい」で獲得し、非港湾企業6社(SGHD・ハマキョウレックス・あかつき本社・
    三井倉庫HD・ダイフク・トーヨーカネツ)から誤った「港湾ロジスティクス」テーマが
    正しく外れることを確認済み。

## 提出手順(運営が行う)

このファイル自体を Notion 台帳へ直接書き込む経路は無い(単語帳の変更経路は
Cursor Automation の提案 → `pnpm biztag gate` の2つだけ。`services/yuho-quant/CLAUDE.md` 参照)。
このファイルは**提案の内容(草稿)**であり、実際に台帳へ「提案(未審査)」として載せるには
Worker の受付エンドポイントを叩く必要がある:

```bash
curl -X POST "$KABULAB_CF_ORIGIN/yuho-quant/vocabulary/proposals" \
  -H "Authorization: Bearer $VOCAB_REVIEW_TOKEN" \
  -H "Content-Type: application/json" \
  --data-binary @services/yuho-quant/src/biztag/proposals/2026-09-26-v5-core-themes.json
```

(`2026-09-25-quality.json`/`2026-09-26-tester-themes.json`/`2026-09-26-round2.json` は
既に採用済み・v4(本番の銘柄マスタ（補足）DB も `単語帳の版=v4` で稼働中を確認済み)。
次に提出すべきは `baseVersion: "v4"` の `2026-09-26-v5-core-themes.json`。)

(`$VOCAB_REVIEW_TOKEN` は `VOCAB_REVIEW_TOKEN_SHA256` のハッシュ元。Cursor Automation の
`KABULAB_CF_VOCAB_TOKEN` と同じ値。ローカルの `.env` には平文トークンを置かない運用のため、
実際のトークン値は運営の手元でのみ扱う。)

台帳に「提案(未審査)」として載ったら、次の `pnpm biztag run`(catchup.yml の日次実行、または
`gh workflow run catchup.yml -f target=biztag` で即時実行)の冒頭で `pnpm biztag gate` が自動審査する。
先に手元で審査を通るか確認したい場合は:

```bash
pnpm biztag gate
```

を(提案が台帳に載った状態で)実行する。関門の中身(§6.3 4項目)は
`docs/005-yuho-quant-business-tags.md` 参照。

## このエージェントが実施した検証(ローカル・jev/Notion/D1 の資格情報なしで可能な範囲)

1. `ProposalSchema.safeParse` — 形式検査 OK。
2. `verifySources`(関門と同じ関数)を実際に一度ネットワーク越しに実行し、
   全 17 件の変更・出典 URL・引用が「日本標準産業分類」PDF の実テキスト
   (unpdf 抽出・NFKC/空白正規化)に実在することを確認済み(issues: 0)。
3. `applyProposal` + `validateVocabulary` — 適用後の単語帳が意味検査を全て通過
   (issues: 0)。適用後は upstream 100/100(上限ちょうど)・downstream 97/100・
   distribution(新設) 10/100。
4. `prefilter` を実データ(golden セットの実引用文・ローカル fixture)に対して
   before/after で実行し、Advantest(6857)に半導体テスタ語が新規候補として付くこと、
   既存 fixture の候補集合が壊れていないことを確認済み。

**未検証(このワークツリーに `TYPESAFE_API_KEY` / `NOTION_TOKEN` / D1 資格情報が無いため実行不能)**:
`pnpm biztag golden`(jev 呼び出しを伴う精度測定)、および 7203/6857/5108 等の実銘柄に対する
dry-run 判定(`pnpm biztag run --dry-run`)。運営側で `.env` を用意した環境で
`pnpm biztag gate` を実行すれば、関門が自動でゴールデンセット再評価まで行う。

## `2026-09-26-round2.json` の検証(このエージェントは Notion/D1/jev の読み取り専用資格情報あり)

1. `ProposalSchema.safeParse` — 形式検査 OK。
2. `applyProposal(v3, round2, "v4")` + `validateVocabulary` — issues 0 件。
   適用後の列内訳は upstream 100/100(変更なし・満杯)・downstream 97/100(変更なし)・
   distribution 15/100(10→15。新設5語はすべて distribution)。変わった語は6件
   (add_business×5 + add_keywords×1)／v3の有効語数256 = **2.3%**(上限20%に対して余裕大)。
3. `verifySources`(関門と同じ関数)を実際に一度ネットワーク越しに実行し、
   全6件の変更・9件の出典URL・引用が「日本標準産業分類」PDF の実テキスト
   (unpdf抽出・NFKC/空白正規化)に実在することを確認済み(issues: 0)。
4. **カバレッジ(prefilterのみ・jev呼び出しなし)**: 銘柄マスタ（補足）から
   `事業タグの状態=判定済` かつ3列とも空の1,130社の「事業の内容」等を実際に取得し、
   v3→v4で `prefilter` を再実行。**312社(27.6%)が新語による候補を新たに獲得**
   (内訳: `B.SVC.SPECIALTY_WHOLESALE` 163社・`B.MOBI.AUTO_PARTS` 101社・
   `B.SVC.LIFESTYLE_SPECIALTY_RETAIL` 24社・`B.SVC.APPAREL_FOOTWEAR_RETAIL` 19社・
   `B.SVC.ADVERTISING_AGENCY` 19社・`B.SVC.MANAGEMENT_CONSULTING` 14社。重複あり)。
   うち17社は候補語が0件→1件以上に転じた(残りは既存の誤フィット候補に加えて
   正しい候補が付いた形)。
5. **jevサンプル判定(dry-run・20社)**: 上記で新候補を獲得した20社を実際にjevへ
   問い合わせた結果、**16社(80%)が新語で「はい」判定**(確率0.64〜0.99)に転じた
   (例: 阪和興業→専門商社・卸売業0.93、しまむら→衣料品・靴の小売専門店0.98、
   博報堂DYホールディングス→広告代理店0.98、トヨタ紡織→自動車部品0.70)。
   1社は要確認止まり、3社は既存語含め全て「いいえ」(誤検知ではなく、候補にはなった
   ものの実際の主業ではないとjevが正しく除外した例を含む)。
6. **ゴールデンセット再評価(`pnpm biztag golden --vocab-file=v4.json`。calibration.json の
   yesMin=0.50・noMax=0.25・jev-1.13.0)**: precisionYes **0.968**(v1較正時 0.962 から
   低下なし・関門の目標0.9以上を満たす)・recallYes 0.757(較正時0.760と同水準)・
   mustHitRecall 0.905(較正時と同一)・mustNotViolations 0・filterMissRate 0。
   新設5語はゴールデンセットに expect ラベルが無いため(§6.3既知の制約どおり)この評価に
   一切寄与しない — 精度への影響は `B.MOBI.AUTO_PARTS` への `add_keywords` のみだが、
   結果は悪化していない。

以上より、関門(§6.3 4項目)は全てローカルで再現・通過を確認済み。

## `2026-09-26-v5-core-themes.json` の検証(利用者報告の全社横断監査。Notion/D1/jev の
読み取り専用資格情報あり)

利用者報告「9987 スズケンで中核の医薬品卸売事業が付かず、周辺事業(物流受託)から
投資テーマ『港湾ロジスティクス』が誤って付く」を受け、判定済 3,607 社全件を
Notion「銘柄マスタ（補足）」から取得し、テーマ監査(構成語ごとの発火数・単独発火数)と
中核事業監査(「事業の内容」の事業区分表からの語彙欠落クラスタ化)を実施した。

### A. テーマ監査(発火数上位より抜粋)

| テーマ | 対象社数 | 所見 |
|---|---|---|
| T.PORT_LOGISTICS 港湾ロジスティクス | 145社 | 構成語のうち `B.LOGI.WAREHOUSING_3PL`(倉庫・3PL)単独発火90社・`B.MACH.MATERIAL_HANDLING`(物流機器・マテハン)単独発火26社、計116社(80%)が港湾と無関係な構成語だけでテーマ付けされていた。両語とも自身の definitionJa/keywords が港湾に触れていない(前者=常温・常圧の普通倉庫全般、後者=工場・倉庫・鉱山向け搬送機器)。**v5で是正(下記)**。 |
| T.ECON_SECURITY_CRITICAL 経済安全保障重要物資 | 206社 | `B.ICT.CLOUD_IAAS_PAAS`(クラウド基盤)単独発火63社。ただし法令(経済安全保障推進法施行令第一条八号)の「クラウドプログラム」定義自体が「他人の情報処理の用に供するシステムに用いるプログラム」と広く、語の定義も法令に忠実。誤りとは言い切れないため本ラウンドでは変更しない(要継続観察)。 |
| T.NATIONAL_RESILIENCE 防災・国土強靱化関連 | 97社 | `B.RE.CIVIL_ENGINEERING`(土木・道路舗装)単独発火32社。テーマの definitionJa 自体が「土木・総合建設・道路・上下水道など国土強靱化に関わる企業群」と明記しており、日本成長戦略の「国土強靱化」分野の定義に沿う。変更しない。 |
| T.DRUG_DISCOVERY 創薬・新薬開発 | 78社 | `B.MED.API`(原薬)・`B.MED.INNOVATOR_DRUG`(新薬)単独発火各16社。API/CDMOは創薬分野の公式ロードマップ項目に明記(既存出典どおり)。変更しない。 |

その他の44テーマ(半導体バリューチェーン・電子部品等)は単独発火の構成語もいずれも
テーマの定義域に収まっており、汎用語の紛れ込みは確認されなかった。

### B. 中核事業監査(語彙欠落クラスタ)

- 判定済3,607社中987社(27%)がタグ0件、うち87社(2.4%)は絞り込みですら候補語が
  1件も無い(=単語帳に対応する語が皆無)。87社の内訳はサービス業28・その他製品10・
  情報・通信業9等で、フィットネスクラブ・学習塾・旅行代理店・M&A仲介・着物レンタル・
  銭湯温浴・保育・コーチング・足場レンタル等、業態ごとに個別の語が必要な**多様なニッチ
  サービスのロングテール**であり、単一パターンでの是正は不可(将来ラウンドでの個別追加が
  必要。本ラウンドでは対応しない)。
- 一方、「事業の内容」に「◯◯卸売事業」という事業区分名を持つ59社のうち46社(78%)が
  `B.SVC.SPECIALTY_WHOLESALE`(専門商社・卸売業)の候補にすらなっていなかった。原因は
  keywords が「卸売業」「卸売販売」等で、有報の実際の表記「卸売事業」(語尾が異なる)に
  当たらないため。この46社には日本の医薬品卸売大手5社中4社
  (メディパルホールディングス7459・アルフレッサホールディングス2784・
  東邦ホールディングス8129・バイタルケーエスケーホールディングス3151。スズケン9987を
  含めれば5社)が含まれており、9987の報告事象と同一原因。**v5で是正(下記)**。

### C. 「明示された事業が要確認止まり」の原因調査(Task D。9987介護0.31 と類似10件をサンプル)

`uncertain` 列の確率降順(0.49に集中)で10件抽出(7701島津製作所・6753シャープ・
1414ショーボンド・4733OBC・4116大日精化・6369トーヨーカネツ・2181パーソルHD・
6706電気興業・4187大阪有機化学・9987スズケン)を根拠文つきで確認した結果、共通パターンは
**「事業の内容」冒頭の一文列挙(「A、B、Cの事業を営んでおります」)や決算概況(MD&A)の
一段落だけに名前が出て、独立した段落・セグメント区分表としての説明が本文に見当たらない
語」ほど確率が0.3〜0.49に留まる**こと。jev の指示文(`judge.ts`の`buildQuestion`)は
「incidental mention(通りすがりの言及)」を明示的に「いいえ」に倒すため、列挙文のみの
言及は事業として実在してもこの基準に触れて確信度が上がらない。**これは単語帳(keywords/
definition)の問題ではなく、抜粋の粒度または質問文言の設計に起因する**。judge.ts の
問い方を変えるとしきい値の再較正(§11.4)が必要になるため、本ラウンドでは変更せず、
運営への申し送り事項として記録する(推奨: 将来的に「事業の内容」の列挙文だけでなく
セグメント表の当該行を最優先で抜粋に含める、または列挙のみの言及への確信度ペナルティを
和らげる指示文の改定を検討)。

### D. v5 変更内容と検証結果

`services/yuho-quant/src/biztag/proposals/2026-09-26-v5-core-themes.json`(baseVersion v4):

1. `update T.PORT_LOGISTICS.members` → `["B.LOGI.PORT_OPERATIONS"]` のみに絞る
   (`B.LOGI.WAREHOUSING_3PL`・`B.MACH.MATERIAL_HANDLING` を除外)。
2. `add_keywords B.SVC.SPECIALTY_WHOLESALE` → `"卸売事業"` を追加。
3. `update B.SVC.SPECIALTY_WHOLESALE.definitionJa/definitionEn` → 例示商品分野に
   「医薬品・医療用品」を追加(ドライランで、キーワード追加だけでは医薬品卸売各社が
   定義文の例示不足により「確認不能」止まりになることを確認したための追加是正)。

検証:
- `ProposalSchema.parse` OK・`applyProposal(v4→v5)` + `validateVocabulary` issues 0件。
  列内訳 upstream 100/100(変更なし)・downstream 97/100(変更なし)・distribution 15/100
  (変更なし。テーマの members 変更・definitionJa/EN 変更・keywords 追加のみで新語は
  無い)。変更語は2件(T.PORT_LOGISTICS・B.SVC.SPECIALTY_WHOLESALE)/有効語261件 = **0.77%**。
- `verifySources`(関門と同じ関数・実ネットワーク越し): 全4件の変更・引用が
  日本標準産業分類PDF・戦略17分野ロードマップPDFの実テキストに実在することを確認済み
  (issues: 0)。
- **ゴールデンセット再評価**(`evaluateGolden`・v4とv5を同一プロセス内で
  `createMemoizedJevClient`共有・calibration.json の yesMin=0.50/noMax=0.25/jev-1.13.0):
  precisionYes v4 **0.9649** → v5 **0.9650**(低下なし)・recallYes 0.7597→0.7624
  (微増)・mustHitRecall 0.9048(同一)・mustNotViolations 0→0・filterMissRate 0→0。
- **ドライラン判定(19社。医薬品卸5社+多業種卸売8社+港湾テーマ剥落候補6社)**:
  実際の書類テキストに対し v5 単語帳で `prefilter`+`judgeCandidates` を実行し、
  Notion本番(v4)の結果と比較。
  - 9987 スズケン: before `down=[SaaS, 倉庫・3PL] themes=[港湾ロジスティクス]` →
    after `down=[SaaS, 倉庫・3PL] dist=[専門商社・卸売業] themes=[]`
    (中核事業が追加され、誤ったテーマが消えた)。
  - 7459 メディパルHD・2784 アルフレッサHD・8129 東邦HD・3151 バイタルケーエスケーHD:
    いずれも `専門商社・卸売業` を新規に「はい」(確率非開示だが yesMin=0.50 以上)で獲得。
  - 9842 アークランズ・8030 中央魚類・9973 KOZOHD・7555 大田花き(要確認止まり)等、
    他業種の卸売企業でも同様に候補〜「はい」化を確認。
  - 港湾と無関係な6社(9143 SGホールディングス・9037 ハマキョウレックス・
    8737 あかつき本社・9302 三井倉庫ホールディングス・6383 ダイフク・
    6369 トーヨーカネツ)から、誤った「港湾ロジスティクス」テーマが**全て**外れることを
    確認(構成語を港湾運送・港湾荷役のみに絞ったことによる意図通りの効果)。

以上より、関門(§6.3 4項目: 形・出典・ゴールデン精度・変更量)は全てローカルで
再現・通過を確認済み。

### 今回対応しなかった事項(運営への申し送り)

- T.ECON_SECURITY_CRITICAL の `B.ICT.CLOUD_IAAS_PAAS` 単独発火63社: 法令の定義自体が
  広いため誤りと断定できないが、一般的なSIer/ホスティング業者まで広く拾っている懸念が
  残る。次回ラウンドで実データサンプルを精査し、必要なら「他社への提供が主要事業か」を
  絞るkeyword/excludeKeywordの追加を検討。
- 987社(27%)がタグ0件、うち87社は候補語も0件(ニッチ業態のロングテール)。業態別に
  新語追加が必要だが1ラウンドでの対応は非現実的。次回以降、出現頻度の高い業態
  (フィットネス・学習塾・旅行代理店・コンサル等)から段階的に追加する。
- 「事業の内容」の列挙文だけに名前がある事業が要確認止まりになる問題(Task D)は
  vocabulary層では解決できない。judge.ts の指示文・抜粋粒度の改定 + §11.4 再較正が
  必要な別プロジェクトとして切り出すことを推奨する。
